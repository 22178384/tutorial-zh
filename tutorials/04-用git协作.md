# 04 · 用 Git 协作

1. Fork 别人的仓库，或自己从 `main` 切 `feature/xxx`。
2. 本地提交：`git add -A && git commit -m "..."`。
3. 推送：`git push -u origin feature/xxx`。
4. 在 GitHub 开 PR，等评审。
5. 评审通过后合并；冲突用 `git merge` / `git rebase` 解决。
